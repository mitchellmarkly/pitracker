services:
  pi-tracker:
    build: .
    container_name: pi-tracker
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - PI_DB_PATH=/data/pi-tracker.db
    volumes:
      - ./data:/data
    restart: unless-stopped
