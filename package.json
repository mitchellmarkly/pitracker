{
  "name": "pi-tracker",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w @pi-tracker/api & npm run dev -w @pi-tracker/web",
    "build": "npm run build -w @pi-tracker/shared && npm run build -w @pi-tracker/api && npm run build -w @pi-tracker/web",
    "test": "npm run test -w @pi-tracker/api",
    "lint": "npm run lint -w @pi-tracker/api && npm run lint -w @pi-tracker/web",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^3.3.3"
  }
}
